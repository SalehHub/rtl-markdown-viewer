{
  "name": "rtl-markdown-viewer",
  "version": "1.2.0",
  "private": true,
  "description": "A local-first Chrome Markdown viewer with real Arabic and mixed RTL/LTR support.",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "check": "node --check src/shared.js && node --check src/content.js && node --check popup/popup.js && node --check tests/integration-server.js && node --test tests/*.test.js",
    "package": "bash scripts/package-extension.sh"
  }
}
